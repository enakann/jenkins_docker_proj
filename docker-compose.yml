version: '2'
services:
     jenkins_test:
        build:
           context: .
           dockerfile: Dockerfile
        volumes:
          - "./app:/app"
          - "./test-reports:/test-reports"
        command: ["sh", "entry_point.sh"]
