version: '2'
services:
     jenkins_test:
        build:
           context: .
           dockerfile: Dockerfile
        volumes:
          - "./app:/app"
          - "./test_reports:/test_reports"
        command: ["sh", "entry_point.sh"]
